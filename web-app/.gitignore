node_modules/
.next/
out/
build/
coverage/
playwright-report/
test-results/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts

# Local scratch / diagnostic material (never commit)
scratch_check_db.ts
.screenshots/
scratch/*.mjs
scratch/*.html
scratch/verify-rag-german.ts

# Generated eval checkpoints only — the eval harness + testset are committed
# so the eval workflow can run in CI.
data/processed/
