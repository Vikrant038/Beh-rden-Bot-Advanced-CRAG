# =======================================================================
# Behoerden-Bot 3.0 — Unified Requirements
# =======================================================================
# This is the SINGLE requirements file for the project.
# The old requirement.txt (pip freeze snapshot) has been consolidated here.
#
# Installation:
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm   # Required for PII name masking
#
# Pinned to exact tested versions from the development environment.
# Minimum-version bounds used for CI compatibility.
# =======================================================================

# --- Core UI & API ---
streamlit==1.59.0
fastapi==0.141.1
uvicorn==0.51.0

# --- LLM Clients ---
groq==1.6.0
openai==2.50.0                 # Used as the client wrapper for Groq (OpenAI-compatible API)
huggingface_hub==1.22.0

# --- Embeddings & ML ---
sentence-transformers==5.6.0
faiss-cpu==1.14.3
torch==2.13.0
transformers==5.13.0
accelerate==1.14.0
numpy==2.5.1

# --- Retrieval ---
rank-bm25==0.2.2               # Sparse BM25 keyword retrieval (Okapi)

# --- Database ---
sqlalchemy==2.0.51             # Async ORM for PostgreSQL
psycopg2-binary==2.9.12        # PostgreSQL driver (sync, required by pgvector)
asyncpg==0.31.0                # Async PostgreSQL driver (used by SQLAlchemy async engine)
pgvector==0.5.0                # pgvector SQLAlchemy type integration

# --- Data Ingestion ---
trafilatura==2.1.0             # Web scraping: HTML -> clean text
pdfplumber==0.11.10            # PDF extraction

# --- Web Search (CRAG Fallback) ---
ddgs==9.14.4                   # DuckDuckGo search (use: from ddgs import DDGS)

# --- Configuration & Utilities ---
python-dotenv==1.2.2
pydantic==2.13.4

# --- Observability ---
langfuse==4.14.1               # Langfuse v4 OTel-native tracing

# --- Resilience ---
pybreaker==1.4.1               # Circuit breaker for Groq API

# --- PII Masking (GDPR Compliance) ---
spacy>=3.7.0                   # NER for person name detection
# After install: python -m spacy download en_core_web_sm

# --- Evaluation ---
datasets==5.0.1                # Used in eval_ragas.py for testset generation
