.env
.venv/
venv/
models/
checkpoints/
*.faiss
*.bin
*.safetensors
*.npy
*.jsonl
__pycache__/
.DS_Store
# Freebuff desktop app state (local-only, churns every session)
.freebuff/

# Local pnpm store (accidentally committed in an earlier session — 2 files
# were removed; keep the whole directory ignored so it never returns)
.pnpm-store/

# Generated eval outputs + old Python CRAG-eval harness (not pushed)
mvp-python/tests/eval_ragas_30.py
mvp-python/data/processed/
scratch/rerank-server.py
scratch/run-webapp-eval.sh
# Web-app eval checkpoints (regenerable) — the testset in web-app/data/eval is
# committed so the eval workflow can run in CI; only the outputs are ignored.
web-app/data/processed/

# Local educational / learning curriculum markdown files
learning/
web-app/learning/

