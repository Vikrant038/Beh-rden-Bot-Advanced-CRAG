# Semgrep ignore rules — see web-app/docs/security/semgrep-backlog.md.
#
# mvp-python/docker-compose.yml (ankane/pgvector) is the dev-only compose for the
# Python/Streamlit reference pipeline. no-new-privileges / writable-filesystem-service
# hardening on a local database container with a bind-mounted volume is
# low-value and can cause spurious boot failures. Documented in
# mvp-python/docs/Postgres_Docker_Setup.md.
/mvp-python/docker-compose.yml
